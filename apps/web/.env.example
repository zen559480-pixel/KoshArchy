# In development, Vite proxies /api to localhost:3001 — VITE_API_URL not needed.
# Set this ONLY for production (Vercel) to point at your Render API URL.
# VITE_API_URL=https://your-api.onrender.com/api
